<?php 
// an associative array is one that uses key names that have been assigned to it.
// the diff btwn this and indexed is that indexed uses numeric indexes starting its count from 0
$class=array("Bruce"=>"24", "Hacker"=>"13", "Enoch"=>"20", "Emily"=>"10",  );
 echo "My name is Bruce and i am".$class['Bruce'];
 // looping through an associative array using a foreach loop
 foreach ($variable as $key => $value) {
 	#code...
 }
 ?>